---
interface Props {
  title: string;
  description: string;
  image: string;
  link: string;
  category?: string;
}

const { title, description, image, link, category = "Finanzas" } = Astro.props;
---

<article class="news-card h-100">
  <div class="card h-100 border-0 shadow-sm">
    <div class="card-img-wrapper">
      <img 
        src={image} 
        class="card-img-top" 
        alt={title}
        loading="lazy"
      />
      {category && (
        <span class="badge-category">{category}</span>
      )}
    </div>
    
    <div class="card-body d-flex flex-column">
      <h3 class="card-title h5 fw-bold mb-3">{title}</h3>
      <p class="card-text text-muted mb-4 flex-grow-1">{description}</p>
      <a href={link} class="btn btn-primary btn-sm align-self-start">
        Leer más
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" fill="currentColor" class="ms-1" viewBox="0 0 16 16">
          <path fill-rule="evenodd" d="M1 8a.5.5 0 0 1 .5-.5h11.793l-3.147-3.146a.5.5 0 0 1 .708-.708l4 4a.5.5 0 0 1 0 .708l-4 4a.5.5 0 0 1-.708-.708L13.293 8.5H1.5A.5.5 0 0 1 1 8z"/>
        </svg>
      </a>
    </div>
  </div>
</article>

<style>
  .news-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    animation: fadeInUp 0.6s ease-out;
  }

  .news-card:hover {
    transform: translateY(-8px);
  }

  .news-card:hover .card {
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.15) !important;
  }

  .card {
    border-radius: 16px;
    overflow: hidden;
    transition: box-shadow 0.3s ease;
  }

  .card-img-wrapper {
    position: relative;
    height: 220px;
    overflow: hidden;
    background-color: #f0f0f0;
  }

  .card-img-top {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .news-card:hover .card-img-top {
    transform: scale(1.08);
  }

  .badge-category {
    position: absolute;
    top: 12px;
    left: 12px;
    background-color: var(--primary-color);
    color: white;
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.5px;
    text-transform: uppercase;
    z-index: 10;
  }

  .card-body {
    padding: 1.75rem;
  }

  .card-title {
    color: var(--text-dark);
    line-height: 1.4;
    transition: color 0.3s ease;
  }

  .news-card:hover .card-title {
    color: var(--primary-color);
  }

  .card-text {
    font-size: 0.95rem;
    line-height: 1.6;
    color: var(--gray-600);
  }

  .btn-primary {
    font-weight: 600;
    padding: 0.5rem 1.25rem;
    border-radius: 8px;
    display: inline-flex;
    align-items: center;
    transition: all 0.3s ease;
  }

  .btn-primary:hover {
    transform: translateX(4px);
    box-shadow: 0 4px 12px rgba(17, 115, 212, 0.3);
  }

  @media (max-width: 768px) {
    .card-img-wrapper {
      height: 180px;
    }
    
    .card-body {
      padding: 1.25rem;
    }
  }
</style>